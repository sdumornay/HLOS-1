{
  "name": "LeaderInterview",
  "type": "object",
  "properties": {
    "organization_id": {
      "type": "string"
    },
    "interviewee_name": {
      "type": "string"
    },
    "interviewee_role": {
      "type": "string"
    },
    "interviewer": {
      "type": "string"
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "strengths_observed": {
      "type": "string"
    },
    "tensions_perceived": {
      "type": "string"
    },
    "leadership_gaps": {
      "type": "string"
    },
    "hopes_for_team": {
      "type": "string"
    },
    "concerns": {
      "type": "string"
    },
    "notable_quotes": {
      "type": "string"
    },
    "coach_observations": {
      "type": "string"
    }
  },
  "required": [
    "organization_id",
    "interviewee_name"
  ],
  "rls": {
    "create": {
      "data.organization_id": "{{user.organization_id}}"
    },
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "lead_pastor"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "coach"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "lead_pastor"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "coach"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "$and": [
            {
              "user_condition": {
                "role": "lead_pastor"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "coach"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    }
  }
}