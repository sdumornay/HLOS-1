{
  "name": "LeaderInterview",
  "type": "object",
  "properties": {
    "organization_id": {
      "type": "string"
    },
    "respondent_email": {
      "type": "string",
      "description": "Email of the leader who submitted the self-assessment"
    },
    "interviewee_name": {
      "type": "string",
      "description": "Leader's name (self-assessment)"
    },
    "interviewee_role": {
      "type": "string"
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "strengths_observed": {
      "type": "string",
      "description": "Perceived strengths"
    },
    "weaknesses": {
      "type": "string",
      "description": "Perceived weaknesses"
    },
    "tensions_perceived": {
      "type": "string"
    },
    "leadership_gaps": {
      "type": "string"
    },
    "hopes_for_team": {
      "type": "string"
    },
    "concerns": {
      "type": "string"
    }
  },
  "required": [
    "organization_id",
    "interviewee_name"
  ],
  "rls": {
    "create": {
      "data.organization_id": "{{user.organization_id}}"
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "coach"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "lead_pastor"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "data.respondent_email": "{{user.email}}"
        },
        {
          "$and": [
            {
              "data.organization_id": "{{user.organization_id}}"
            },
            {
              "created_by_id": "{{user.id}}"
            }
          ]
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "coach"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "lead_pastor"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        }
      ]
    }
  }
}