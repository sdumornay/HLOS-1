{
  "name": "CommAgreement",
  "type": "object",
  "properties": {
    "organization_id": {
      "type": "string"
    },
    "agreement_text": {
      "type": "string"
    },
    "category": {
      "type": "string",
      "enum": [
        "meeting_norms",
        "conflict_protocol",
        "feedback",
        "decision_making",
        "communication_channels",
        "other"
      ],
      "default": "meeting_norms"
    },
    "agreed_by": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Emails of team members who agreed"
    },
    "status": {
      "type": "string",
      "enum": [
        "proposed",
        "active",
        "retired"
      ],
      "default": "proposed"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "organization_id",
    "agreement_text"
  ],
  "rls": {
    "create": {
      "data.organization_id": "{{user.organization_id}}"
    },
    "read": {
      "data.organization_id": "{{user.organization_id}}"
    },
    "update": {
      "$or": [
        {
          "$and": [
            {
              "user_condition": {
                "role": "lead_pastor"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "coach"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "$and": [
            {
              "user_condition": {
                "role": "lead_pastor"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "coach"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    }
  }
}