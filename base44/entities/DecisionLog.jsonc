{
  "name": "DecisionLog",
  "type": "object",
  "properties": {
    "organization_id": {
      "type": "string"
    },
    "session_id": {
      "type": "string",
      "description": "Reference to session where decision was made"
    },
    "issue_question": {
      "type": "string",
      "description": "The issue or question that prompted the decision"
    },
    "decision": {
      "type": "string",
      "description": "The decision made"
    },
    "context": {
      "type": "string",
      "description": "Why this decision was made"
    },
    "made_by": {
      "type": "string",
      "description": "Person or group who made the decision"
    },
    "participants": {
      "type": "string",
      "description": "Who was involved in the decision discussion"
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "category": {
      "type": "string",
      "enum": [
        "financial",
        "staffing",
        "ministry",
        "strategy",
        "operations",
        "communications",
        "other"
      ],
      "default": "ministry"
    },
    "impact": {
      "type": "string",
      "enum": [
        "low",
        "medium",
        "high",
        "critical"
      ],
      "default": "medium"
    },
    "affected_teams": {
      "type": "string"
    },
    "resulting_actions": {
      "type": "string",
      "description": "What actions resulted from this decision"
    },
    "review_date": {
      "type": "string",
      "format": "date"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "reversed",
        "superseded"
      ],
      "default": "active"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "organization_id",
    "decision",
    "made_by",
    "date"
  ],
  "rls": {
    "create": {
      "data.organization_id": "{{user.organization_id}}"
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "coach"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "data.organization_id": "{{user.organization_id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "coach"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "lead_pastor"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "$and": [
            {
              "user_condition": {
                "role": "lead_pastor"
              }
            },
            {
              "data.organization_id": "{{user.organization_id}}"
            }
          ]
        }
      ]
    }
  }
}